version: 0.0
os: linux

files:
  - source: /
    destination: /var/rerekisho_reaction_app
    overwrite: true

permissions:
  - object: /var/rerekisho_reaction_app
    pattern: "**"
    owner: ec2-user
    group: ec2-user
    mode: 755
    type:
      - file
      - directory

hooks:
  # ApplicationStop:
  #   - location: scripts/stop.sh
  #     timeout: 60
  #     runas: root

  # BeforeInstall:
  #   - location: scripts/before_install.sh
  #     timeout: 300
  #     runas: root

  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 600
      runas: root

  # ApplicationStart:
  #   - location: scripts/start.sh
  #     timeout: 60
  #     runas: root

  # ValidateService:
  #   - location: scripts/validate.sh
  #     timeout: 60
  #     runas: root